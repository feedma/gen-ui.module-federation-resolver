{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {},
  "devDependencies": {
    "lerna": "^8.2.3"
  },
  "scripts": {
    "build": "npx lerna run build",
    "publish:local": "npx lerna run publish:local"
  }
}
